diff --git a/src/core.cppm b/src/core.cppm
index 4059650..8b230d8 100644
--- a/src/core.cppm
+++ b/src/core.cppm
@@ -39,5 +39,16 @@ module;
 
 export module boost.core;
 
+// 对象宏 re-homing (M3): 宏无法跨模块导出。上游 <boost/version.hpp> 的对象宏
+// 以 boost:: 命名空间 constexpr 重置于此 (拼写保持): 消费者 import 后可用
+// boost::BOOST_VERSION。值须与 deps/boost/boost/version.hpp 一致 — tests/macros.cpp
+// 在旁路头 (include/boost-module/macros.hpp) 参与下交叉校验。
+// 注意: 同一 TU 中若定义了同名宏 (include <boost-module/macros.hpp>), 宏展开会
+// 吞掉 boost::BOOST_VERSION 拼写 (→ boost::109100), 两种拼写互斥, 二选一。
+export namespace boost {
+  constexpr int BOOST_VERSION = 109100;
+  constexpr const char* BOOST_LIB_VERSION = "1_91";
+}
+
 #include "gen_exports/core.inc"
 
diff --git a/src/gen_exports/core.inc b/src/gen_exports/core.inc
index e8e2d79..8f30e11 100644
--- a/src/gen_exports/core.inc
+++ b/src/gen_exports/core.inc
@@ -160,7 +160,10 @@ export namespace boost { namespace core { namespace detail {
   using boost::core::detail::bit_ceil_impl;
   using boost::core::detail::byteswap_impl;
   using boost::core::detail::class_template_name;
+#if defined(__GNUC__)
+  // M3 hand-guard: boost/core/cmath.hpp defines copysign_impl only on gcc-like compilers.
   using boost::core::detail::copysign_impl;
+#endif
   using boost::core::detail::countl_impl;
   using boost::core::detail::countr_impl;
   using boost::core::detail::fix_typeid_name;
@@ -174,8 +177,15 @@ export namespace boost { namespace core { namespace detail {
   using boost::core::detail::popcount_impl;
   using boost::core::detail::sequence_template_name;
   using boost::core::detail::set_template_name;
+#if defined(_WIN32) || defined(__WIN32__) || defined(__CYGWIN__)
+  // M6 platform guard: sp_thread_sleep/yield live in boost::core::detail only on Windows;
+  // on POSIX (nanosleep/sched_yield branch of the header) they are declared in boost::core.
   using boost::core::detail::sp_thread_sleep;
   using boost::core::detail::sp_thread_yield;
+#else
+  using boost::core::sp_thread_sleep;
+  using boost::core::sp_thread_yield;
+#endif
   using boost::core::detail::tn_add_each;
   using boost::core::detail::tn_add_each_impl;
   using boost::core::detail::tn_holder;
